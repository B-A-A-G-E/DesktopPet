# ![icon](./pet/Mutumi/icon.png) Mutumi

示例桌宠 `Mutumi`

## 安装

``` shell
pet -i -pet Mutumi # 未实现
```

## 环境依赖

### 插件

- `idle`
- `move-randomly`
- `stroke`
- `drag`
- `use-fan`

``` shell
pet -i -plugin idle move-randomly stroke drag use-fan # 暂未实现
```

## `pet/` 配置

### config

``` json
"Mutumi": "./pet/Mutumi" // 或自定义的路径
```


