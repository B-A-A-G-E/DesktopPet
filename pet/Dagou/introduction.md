示例桌宠 `大狗叫`

## 安装

``` shell
pet -i -pet 大狗 # 未实现
```

## 环境依赖

### 插件

- `dagou-integ-plugin`

``` shell
pet -i -plugin idle move-randomly stroke drag use-fan # 暂未实现
```

## `pet/` 配置

### config

``` json
"大狗": "./pet/Dagou" // 或自定义的路径
```


